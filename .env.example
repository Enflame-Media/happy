# Happy App Environment Variables
#
# IMPORTANT: Expo Environment Variable Rules
# ==========================================
# Expo's Metro bundler ONLY bundles variables with the EXPO_PUBLIC_ prefix
# into the client app. Variables without this prefix will be UNDEFINED
# in production builds!
#
# Type: EXPO_PUBLIC_* (client-bundled)
# Type: EAS_BUILD_* (build-time only)
# Type: other (server-side only)
#
# Copy this file to .env and fill in your values.

# ============================================
# Client-Exposed Variables (EXPO_PUBLIC_*)
# ============================================
# These are bundled into the app at build time and accessible via process.env
# WARNING: These values are visible in the compiled JavaScript bundle

# Type: string (URL)
# Default: https://happy-api.enflamemedia.com
# Format: https://host (no trailing slash)
# Note: Baked into app at build time - requires rebuild to change
EXPO_PUBLIC_HAPPY_SERVER_URL=https://happy-api.enflamemedia.com

# Type: boolean
# Default: false
# Valid: true, false
# Note: Enables additional console logging in development
# EXPO_PUBLIC_DEBUG=true

# ============================================
# DEBUG - Use with extreme caution
# ============================================

# Type: boolean
# Default: false
# Valid: true, false
# WARNING: Exposes session data to server - NEVER enable in production builds
# Note: Only use in local development for AI-assisted debugging
# EXPO_PUBLIC_DANGEROUSLY_LOG_TO_SERVER_FOR_AI_AUTO_DEBUGGING=true

# ============================================
# Server-Only Variables (NO prefix needed)
# ============================================
# These are used during EAS Build or development server only.
# They are NOT bundled into the client app.
#
# Examples (if needed in future):
# - EAS_BUILD_* variables for build configuration
# - CI/CD pipeline variables
# - Build-time secrets that shouldn't be in client bundle

# Type: string
# Default: none
# Note: Set automatically by EAS Build
# EAS_BUILD_PROFILE=

# Type: boolean
# Default: false
# Note: Set automatically in CI environments
# CI=
